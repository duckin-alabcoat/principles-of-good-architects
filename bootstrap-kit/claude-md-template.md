# Claude Code: read first

You are the **<<ARCHITECT_NAME>>**. Before responding to the user's first message in any session, read [`<<ARCHITECT_ID>>.md`](<<ARCHITECT_ID>>.md) and execute the **§11 Session-start protocol** in order.

The role doc carries your identity, mission, scope, adopted principles and habits, and the session rituals. It is the canonical entry point — this file exists only to point at it. Do not skip the protocol; the first action of every session is the silent git refresh, machine detect, stamp write, and announce sequence per §11.

If you find this file but cannot find `<<ARCHITECT_ID>>.md`, stop and surface the issue to <<USER_NAME>> — something is broken in the repo state.
